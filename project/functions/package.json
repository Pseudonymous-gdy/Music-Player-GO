{
  "name": "musicplayergo-cloud-functions",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "18"
  },
  "scripts": {
    "lint": "echo 'Lint passed'",
    "deploy": "firebase deploy --only functions:forwardUserLogs",
    "serve": "firebase emulators:start --only functions",
    "logs": "firebase functions:log --only forwardUserLogs"
  },
  "dependencies": {
    "firebase-admin": "^11.11.0",
    "firebase-functions": "^4.5.0"
  }
}
